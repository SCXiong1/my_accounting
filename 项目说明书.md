# 个人记账应用 项目说明书

## 阅读指引

本文档既是项目分析说明，也设计为**可直接用于指导 Claude Code 构建项目的规格书**。按章节顺序阅读可形成完整的工程认知，后续编码时直接对照文档中的接口定义、数据模型和设计模式来实现。

---

## 一、项目概览

### 1.1 这是什么

一个**移动端个人支出记账 Web 应用**，部署在自己的服务器/NAS 上，通过手机浏览器访问。功能覆盖：记录每笔花销、自定义分类和标签、多维度统计分析消费习惯。

- 核心定位：**轻量、移动优先、个人/家庭支出追踪工具**
- 用户：自己和老婆两人使用，各自独立账号
- 目标：快速记账、清晰分析，不搞复杂功能

### 1.2 技术栈

| 层 | 选型 | 理由 |
|----|------|------|
| 后端语言 | Python 3.12+ | 开发效率高，生态成熟 |
| Web 框架 | FastAPI | 高性能异步、自动生成 API 文档、类型校验 |
| ORM | SQLAlchemy 2.0 | Python 最成熟的 ORM，异步支持好 |
| 数据库迁移 | Alembic | SQLAlchemy 官方迁移工具 |
| 数据库 | SQLite 3 | 零配置，数据即文件，备份即复制 |
| 认证 | JWT（python-jose） | 无状态认证，适合单体应用 |
| 密码哈希 | bcrypt | 安全可靠 |
| 前端框架 | Vue 3 + Vant 4 | 轻量移动端 UI，按需引入 |
| UI 组件 | Vant 4（移动端 PWA） | 添加到桌面使用，兼容 Android/iOS |
| 图表库 | ECharts | 饼图、柱状图、折线图 |
| 状态管理 | Pinia | Vue 3 官方推荐 |
| 路由 | Vue Router 4 | SPA 路由 |
| HTTP | Axios | 请求拦截 + JWT 自动附加 |
| PWA | vite-plugin-pwa | manifest + Service Worker |
| 构建工具 | Vite | 极速 HMR |
| 部署 | Docker 或直接运行 | 一行命令启动 |

### 1.3 项目结构

```
backend/
  main.py                      # 应用入口，FastAPI 实例创建
  config.py                    # 配置管理（环境变量覆盖 + _DEFAULTS 内置默认值）
  database.py                  # 数据库连接 + 会话管理（SQLite 外键约束启用）
  conftest.py                  # 测试配置（pytest fixture + test.db 隔离）
  test_api.py                  # 26 个 API 集成测试
  models/                      # SQLAlchemy 数据模型
    mixins.py                  #   TimestampMixin + SoftDeleteMixin（共享列定义）
    user.py
    expense_category.py
    expense_tag.py
    expense.py
    expense_tag_index.py
  schemas/                     # Pydantic 请求/响应模型
    base.py                    #   BaseSchema（from_attributes=True 基类）
    auth.py
    user.py
    category.py
    tag.py
    expense.py
    statistics.py
  api/                         # API 路由（Handler）
    __init__.py                #   APIRouter 总注册
    auth.py                    #   注册/登录/刷新令牌/忘记密码
    users.py                   #   用户信息
    categories.py              #   分类 CRUD + 排序
    tags.py                    #   标签 CRUD + 排序
    expenses.py                #   支出 CRUD + 列表查询 + 恢复
    statistics.py              #   统计接口（含 _parse_ids 参数解析）
  services/                    # 业务逻辑层
    auth_service.py            #   注册（含预设分类）/登录/忘记密码/刷新
    user_service.py            #   个人信息
    category_service.py        #   分类 CRUD（GROUP BY 批量统计）
    tag_service.py             #   标签 CRUD（同名检查 + GROUP BY 批量统计）
    expense_service.py         #   支出 CRUD（_validate_tags 校验 + 智能搜索）
    statistics_service.py      #   统计聚合（monthly 拆分为 5 函数）
  middleware/                   # 中间件
    jwt_auth.py                #   JWT 认证
    error_handler.py           #   全局异常处理（AppException 体系）
  utils/                       # 工具函数
    security.py                #   JWT 生成/验证、密码哈希

frontend/
  src/
    main.ts                    # 入口
    App.vue                    # 根组件（router-view + Tabbar）
    styles/
      global.css               # 全局 CSS + Vant CSS 变量覆盖
    composables/               # 可组合逻辑（Vue 3 composables）
      useECharts.ts            #   图表 init/dispose/resize 生命周期管理
      usePeriodFilter.ts       #   时间范围选择状态机（本月/近N月/今年/自定义）
    stores/                    # 状态管理（Pinia）
      auth.ts                  #   登录状态
      category.ts              #   分类数据
      tag.ts                   #   标签数据
      expense.ts               #   支出数据（游标分页 + 使用 Pick<> 派生 Brief 类型）
      statistics.ts            #   统计数据（byCategory/byTag/monthly）
    views/                     # 页面组件
      LoginPage.vue            #   登录
      RegisterPage.vue         #   注册
      HomePage.vue             #   首页概览
      ExpenseListPage.vue      #   支出列表（初始15条 + 翻页10条无限滚动）
      ExpenseFormPage.vue      #   记账表单（新增/编辑复用）
      CategoryManagePage.vue   #   分类管理
      TagManagePage.vue        #   标签管理
      StatisticsPage.vue       #   统计分析（4图表：useECharts管理生命周期）
      ProfilePage.vue          #   个人信息
      TrashPage.vue            #   回收站（查看+恢复已删除支出）
    components/                # 通用组件
      AmountField.vue          #   金额输入（元→分转换）
      CategoryPicker.vue       #   分类选择器
      TagCheckbox.vue          #   标签多选
      ExpenseCard.vue          #   支出列表卡片
      NotifyBar.vue            #   通知栏（成功/错误/提示）
    core/                      # 纯业务逻辑
      format.ts                #   金额格式化（centsToYuan/yuanToCents/formatAmount）
      time.ts                  #   日期格式化（formatDate/formatShortDate 含年份）
    lib/                       # 基础设施
      api.ts                   #   Axios 实例 + JWT 拦截器（401 跳转）+ buildQueryParams()
      token.ts                 #   Token localStorage 存取
      error.ts                 #   getErrorMessage 错误提取
      feedback.ts              #   通知系统（showSuccess/showError/showTip）
    router/
      index.ts                 # 路由定义 + 导航守卫

data/                          # 数据目录（运行时，gitignore）
  ezexpense.db                 #   SQLite 数据库文件

config.yaml                    # 配置文件
```

---

## 二、需求规格

### 2.1 用户画像

想掌控自己日常开支的个人/小家庭用户，有一定技术能力（能部署个 Python 应用），对数据隐私在意。使用场景：日常随手记一笔花销、月底看看分项统计、年底回顾全年消费。

### 2.2 功能需求（分三阶段）

**P0 - 核心必须：**
- 用户注册/登录（JWT 认证）
- 支出分类 CRUD（预设默认分类 + 自定义），单层扁平结构
- 自定义标签管理
- 支出记录 CRUD：金额、分类、时间、备注、标签（每笔可打多个标签）
- 支出列表查询：按时间范围、分类、标签、关键词筛选 + 游标分页

**P1 - 好用必须：**
- 首页概览：今日/本周/本月/今年支出汇总
- 统计图表：
  - 按分类支出占比（饼图），支持按时间范围 + 标签筛选
  - 按标签支出占比（饼图），支持按时间范围 + 分类筛选
  - 月度趋势（柱状图/折线图），支持按分类 + 标签筛选
- 数据导出 CSV

**P2 - 锦上添花：**
- PWA 离线缓存
- 支出预算提醒
- 数据备份/恢复

### 2.3 非功能需求

| 维度 | 要求 |
|------|------|
| 性能 | 树莓派 1GB 内存流畅运行，API 响应 < 200ms |
| 安全性 | 密码 bcrypt 哈希、JWT 过期、用户数据隔离 |
| 移动体验 | 移动端 PWA，触控友好，可添加到桌面 |
| 可部署性 | 单命令启动，无外部依赖 |
| 数据隐私 | 所有数据本地存储 |

---

## 三、数据模型设计

> 金额以「分」为单位用整数存储（int64），时间用 Unix 时间戳（秒），软删除用 `deleted` 标记 + `deleted_at` 时间戳。

### 3.1 用户表 (user)

```sql
CREATE TABLE user (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    username        VARCHAR(32)  NOT NULL UNIQUE,
    password        VARCHAR(60)  NOT NULL,           -- bcrypt 哈希
    nickname        VARCHAR(64)  NOT NULL DEFAULT '',
    created_at      INTEGER NOT NULL,
    updated_at      INTEGER NOT NULL
);
```

> 注：SQLite 使用 INTEGER 统一存储整数。SQLAlchemy 模型中所有整数字段使用 `Integer` 类型（非 BigInteger/SmallInteger），以确保 SQLite 的 AUTOINCREMENT 正常工作。

### 3.2 支出分类表 (expense_category)

```sql
CREATE TABLE expense_category (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    uid             INTEGER NOT NULL,                 -- 所属用户
    name            VARCHAR(32) NOT NULL,            -- 分类名称
    icon            VARCHAR(10) NOT NULL DEFAULT '📦', -- 图标（emoji）
    color           VARCHAR(7)  NOT NULL DEFAULT '#607D8B', -- 颜色
    display_order   INTEGER NOT NULL DEFAULT 0,
    deleted         INTEGER NOT NULL DEFAULT 0,
    created_at      INTEGER NOT NULL,
    updated_at      INTEGER NOT NULL,
    deleted_at      INTEGER DEFAULT 0,

    FOREIGN KEY (uid) REFERENCES user(id)
);
CREATE INDEX idx_category_uid_deleted ON expense_category(uid, deleted, display_order);
```

预设分类数据（注册时自动创建）：
| 名称 | icon | color |
|------|------|-------|
| 餐饮 | 🍽️ | #FF5722 |
| 交通 | 🚗 | #2196F3 |
| 购物 | 🛒 | #FF9800 |
| 住房 | 🏠 | #795548 |
| 娱乐 | 🎮 | #9C27B0 |
| 医疗 | 💊 | #4CAF50 |
| 教育 | 📚 | #009688 |
| 其他 | 📦 | #607D8B |

### 3.3 标签表 (expense_tag)

```sql
CREATE TABLE expense_tag (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    uid             INTEGER NOT NULL,
    name            VARCHAR(32) NOT NULL,
    display_order   INTEGER NOT NULL DEFAULT 0,
    deleted         INTEGER NOT NULL DEFAULT 0,
    created_at      INTEGER NOT NULL,
    updated_at      INTEGER NOT NULL,
    deleted_at      INTEGER DEFAULT 0,

    FOREIGN KEY (uid) REFERENCES user(id)
);
CREATE INDEX idx_tag_uid_deleted ON expense_tag(uid, deleted, display_order);
```

### 3.4 支出表 (expense)

```sql
CREATE TABLE expense (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    uid             INTEGER NOT NULL,
    amount          INTEGER NOT NULL,                 -- 金额（分）
    category_id     INTEGER NOT NULL,                 -- 分类 ID
    transaction_time INTEGER NOT NULL,                -- 交易时间（Unix 时间戳）
    timezone_offset INTEGER NOT NULL DEFAULT 480,     -- 时区偏移（分钟，默认 UTC+8）
    note            VARCHAR(255) NOT NULL DEFAULT '', -- 备注
    deleted         INTEGER NOT NULL DEFAULT 0,
    created_at      INTEGER NOT NULL,
    updated_at      INTEGER NOT NULL,
    deleted_at      INTEGER DEFAULT 0,

    FOREIGN KEY (uid) REFERENCES user(id),
    FOREIGN KEY (category_id) REFERENCES expense_category(id)
);

CREATE INDEX idx_expense_uid_deleted_time ON expense(uid, deleted, transaction_time);
CREATE INDEX idx_expense_uid_deleted_category ON expense(uid, deleted, category_id, transaction_time);
```

### 3.5 支出标签关联表 (expense_tag_index)

多对多关联，一笔支出可以有多个标签。

```sql
CREATE TABLE expense_tag_index (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    uid             INTEGER NOT NULL,
    expense_id      INTEGER NOT NULL,
    tag_id          INTEGER NOT NULL,
    created_at      INTEGER NOT NULL,

    FOREIGN KEY (uid) REFERENCES user(id),
    FOREIGN KEY (expense_id) REFERENCES expense(id),
    FOREIGN KEY (tag_id) REFERENCES expense_tag(id),

    UNIQUE (expense_id, tag_id)
);
CREATE INDEX idx_tag_index_uid_tag ON expense_tag_index(uid, tag_id, expense_id);
CREATE INDEX idx_tag_index_expense ON expense_tag_index(expense_id);
```

### 3.6 关键设计规则

1. **金额用整数（分）**：`100.50 元 → 10050`。永远不要用浮点数存金额。
2. **时间戳用 Unix 秒**：所有时间字段都是整数 Unix timestamp。展示时按用户时区转换。
3. **软删除**：核心表用 `deleted` + `deleted_at`。查询时始终加 `deleted = 0` 条件。
4. **用户隔离**：所有数据表带 `uid`，所有查询加 `uid = ?` 条件。
5. **标签多对多**：通过 `expense_tag_index` 关联，创建/修改支出时同步维护关联表。删除支出/标签时保留关联记录以便恢复。
6. **标签名唯一**：同一用户下标签名不可重复，创建同名标签返回 400 错误。

---

## 四、API 接口规范

### 4.1 通用约定

- **Base URL**：`/api/v1`（需认证）或 `/api`（认证接口）
- **认证方式**：JWT，通过 Header `Authorization: Bearer <token>` 传递
- **请求格式**：GET 用 query string，POST/PUT 用 `Content-Type: application/json`
- **响应格式**：
  - 成功：直接返回 JSON 数据
  - 错误：`{"detail": "错误描述信息"}`（FastAPI 默认格式）
- **分页**：列表接口使用游标分页，参数 `cursor`（上一页最后一条的 ID）+ `limit`（默认 20）

### 4.2 认证接口

```
POST /api/auth/register
  Request:  {"username": "xxx", "password": "xxx", "nickname": "xxx", "email": "a@b.com"}
  Response: {"token": "...", "user": {...用户信息含预设分类}}
  说明：注册时自动创建 8 个预设支出分类，email 必填用于忘记密码验证

POST /api/auth/forgot-password
  Request:  {"username": "xxx", "email": "xxx", "new_password": "xxx"}
  Response: {"message": "密码重置成功，请使用新密码登录"}
  说明：验证用户名+邮箱匹配后重置密码

POST /api/auth/login
  Request:  {"username": "xxx", "password": "xxx"}
  Response: {"token": "...", "user": {...用户信息}}

POST /api/auth/refresh
  Header:   Authorization: Bearer <当前JWT>
  Response: {"token": "新JWT"}
```

### 4.3 用户接口

```
GET  /api/v1/user/profile
  Response: {"user": {"id": 1, "username": "...", "nickname": "...", ...}}

PUT  /api/v1/user/profile
  Request:  {"nickname": "新昵称", "password": "新密码（可选）", "old_password": "旧密码（改密码时必填）"}
  Response: {"user": {...更新后用户信息}}
```

### 4.4 分类接口

```
GET  /api/v1/categories
  Response: [{"id": 1, "name": "餐饮", "icon": "🍽️", "color": "#FF5722",
              "display_order": 0, "expense_count": 42, "total_amount": 123450}]
  说明：expense_count 和 total_amount 为当前用户的支出统计（非软删除支出）

POST /api/v1/categories
  Request:  {"name": "宠物", "icon": "🐱", "color": "#FF4081"}
  Response: {分类对象}

PUT  /api/v1/categories/:id
  Request:  {"name": "...", "icon": "...", "color": "..."}
  Response: {分类对象}

DELETE /api/v1/categories/:id
  Response: {"deleted": true}
  说明：软删除，已有支出的分类不允许删除，返回 400

PUT  /api/v1/categories/sort
  Request:  {"orders": [{"id": 1, "display_order": 0}, {"id": 2, "display_order": 1}]}
  Response: [排序后的分类列表]
```

### 4.5 标签接口

```
GET  /api/v1/tags
  Response: [{"id": 1, "name": "必买", "display_order": 0, "expense_count": 15}]

POST /api/v1/tags
  Request:  {"name": "冲动消费"}
  Response: {标签对象}
  说明：同名标签返回 400 错误，禁止创建重复标签

PUT  /api/v1/tags/:id
  Request:  {"name": "新名称"}
  Response: {标签对象}

DELETE /api/v1/tags/:id
  Response: {"deleted": true}
  说明：软删除，保留 expense_tag_index 关联记录以便恢复

PUT  /api/v1/tags/sort
  Request:  {"orders": [{"id": 1, "display_order": 0}, {"id": 2, "display_order": 1}]}
  Response: [排序后的标签列表]
```

### 4.6 支出接口（核心）

```
GET  /api/v1/expenses
  Query:
    cursor?          游标（上一页最后一条 ID，首页不传）
    limit?           每页条数（默认 20，最大 100）
    start_time?      起始时间（Unix 秒）
    end_time?        结束时间（Unix 秒）
    category_id?     分类 ID 筛选
    tag_id?          标签 ID 筛选
    keyword?         备注关键词搜索
  Response: {
    "items": [{支出对象含分类和标签详情}],
    "next_cursor": 下一页游标（无更多数据时为 null）,
    "total": 筛选条件下的总数
  }
  支出对象结构：
  {
    "id": 1,
    "amount": 3500,           // 35.00 元
    "category": {"id": 1, "name": "餐饮", "icon": "🍽️", "color": "#FF5722"},
    "tags": [{"id": 1, "name": "午餐"}, {"id": 2, "name": "工作餐"}],
    "transaction_time": 1715678901,
    "timezone_offset": 480,
    "note": "和同事吃湘菜"
  }

GET  /api/v1/expenses/:id
  Response: {单条支出对象}

POST /api/v1/expenses
  Request:  {
    "amount": 3500,           // 35.00 元（分）
    "category_id": 1,
    "tag_ids": [1, 2],
    "transaction_time": 1715678901,
    "timezone_offset": 480,   // 可选，默认 480（UTC+8）
    "note": "和同事吃湘菜"
  }
  Response: {创建的支出对象}

PUT  /api/v1/expenses/:id
  Request:  同创建（所有字段可选，传了就更新）
  Response: {更新后的支出对象}

DELETE /api/v1/expenses/:id
  Response: {"deleted": true}
  说明：软删除，保留标签关联以便回收站恢复后标签不丢失
```

### 4.7 统计接口

```
GET  /api/v1/statistics/overview
  Response: {
    "today": 5000,          // 今日支出（分）
    "this_week": 35000,     // 本周支出
    "this_month": 120000,   // 本月支出
    "this_year": 450000     // 今年支出
  }

GET  /api/v1/statistics/by_category
  Query:
    start_time?     起始时间
    end_time?       结束时间
    tag_ids?        标签筛选（逗号分隔的 ID 列表）
  Response: [{
    "category_id": 1,
    "category_name": "餐饮",
    "category_icon": "🍽️",
    "category_color": "#FF5722",
    "total_amount": 120000,     // 分类支出总额（分）
    "percentage": 28.5,         // 占比（%）
    "transaction_count": 35     // 笔数
  }]

GET  /api/v1/statistics/by_tag
  Query:
    start_time?       起始时间
    end_time?         结束时间
    category_ids?     分类筛选（逗号分隔的 ID 列表）
  Response: [{
    "tag_id": 1,
    "tag_name": "午餐",
    "total_amount": 45000,
    "percentage": 12.3,
    "transaction_count": 15
  }]

GET  /api/v1/statistics/monthly
  Query:
    start_year?      起始年份（默认今年）
    start_month?     起始月份（默认 1）
    end_year?        结束年份
    end_month?       结束月份
    category_ids?    分类筛选（逗号分隔）
    tag_ids?         标签筛选（逗号分隔）
  Response: [{
    "year": 2026,
    "month": 5,
    "total_amount": 120000,     // 当月总额
    "transaction_count": 42,    // 当月笔数
    "by_category": [{           // 当月按分类明细
      "category_id": 1,
      "category_name": "餐饮",
      "category_icon": "🍽️",
      "category_color": "#FF5722",
      "amount": 35000
	    "by_tag": [{               // 当月按标签明细
	      "tag_id": 1,
	      "tag_name": "午餐",
	      "amount": 15000
	    }],
    }]
  }]
  说明：没有支出的月份不返回
```

---

## 五、后端架构设计

### 5.1 分层架构

```
HTTP 请求
  → FastAPI Router（路由注册，api/__init__.py）
  → Middleware（CORS → JWT 认证 → 全局异常处理）
  → API Handler（api/*.py：参数校验（Pydantic）、调用 Service）
  → Service（services/*.py：业务逻辑、事务管理）
  → Repository / SQLAlchemy Session（数据访问、查询）
  → Database（SQLite）
```

**分层规则**：
- API 层：只做 Pydantic 参数校验和调用 Service，不写业务逻辑
- Service 层：包含所有业务规则（如创建支出时维护标签关联、删除分类时检查是否有支出），管理数据库事务
- 数据访问层：使用 SQLAlchemy Session 直接操作，封装常用查询方法
- Models 层：SQLAlchemy ORM 模型定义
- Schemas 层：Pydantic 请求/响应模型，与 API 一一对应

### 5.2 错误处理

使用 FastAPI 的异常机制 + 全局 exception handler：

```python
# 自定义异常
class AppException(Exception):
    def __init__(self, status_code: int, detail: str):
        self.status_code = status_code
        self.detail = detail

# 常用场景
class NotFoundException(AppException):
    def __init__(self, entity: str):
        super().__init__(404, f"{entity}不存在")

class ForbiddenException(AppException):
    def __init__(self):
        super().__init__(403, "无权操作此资源")

class BadRequestException(AppException):
    def __init__(self, detail: str):
        super().__init__(400, detail)
```

全局 handler 捕获 `AppException` 并返回统一格式的 JSON。

### 5.3 JWT 认证流程

```
1. 注册/登录成功 → 后端生成 JWT（payload: {uid, exp}） → 返回给前端
2. 前端存储 token（localStorage）
3. 每次 API 请求带 Header: Authorization: Bearer <token>
4. JWT 中间件从 Header 解析 token → 验证签名和过期时间 → 注入当前 uid
5. 所有 handler 通过依赖注入获取当前 uid，确保用户隔离
```

JWT 过期时间默认 7 天，支持通过 refresh 接口续期。

### 5.4 事务管理

Service 层关键操作使用数据库事务：

```python
# 创建支出的事务示意
async def create_expense(uid: int, data: ExpenseCreate, db: AsyncSession) -> Expense:
    async with db.begin():
        # 1. 写入 expense 记录
        # 2. 写入 expense_tag_index 关联
        # 任一步骤异常则整体回滚
```

关键事务场景：
- 创建/修改支出 + 标签关联维护
- 删除标签 + 清除关联
- 用户注册 + 预设分类创建

### 5.5 配置管理

使用 YAML 配置文件 + 环境变量覆盖：

```yaml
# config.yaml
server:
  host: "0.0.0.0"
  port: 8080

database:
  path: "./data/ezexpense.db"

security:
  jwt_secret: "change-me-in-production"
  jwt_expire_days: 7

cors:
  origins: ["*"]
```

环境变量覆盖规则：`APP_SERVER_PORT=9090` 对应 `config.yaml` 中的 `server.port`。

---

## 六、前端架构设计

### 6.1 移动端 PWA 架构

前端为纯移动端 SPA + PWA，通过 `manifest.json` + Service Worker 支持添加到手机桌面。移动优先设计，无需设备检测分流。

核心页面：
```
/login              →  登录
/register           →  注册
/                   →  首页概览（今日/周/月/年汇总）
/expenses           →  支出列表（筛选、分页）
/expenses/add       →  记账表单
/expenses/:id/edit  →  编辑支出
/categories         →  分类管理
/tags               →  标签管理
/statistics         →  统计分析（多维度图表）
/profile            →  个人信息/修改密码
```

### 6.2 前端分层

```
Pages（页面组件，纯模板 + 调用 Store）
  → Stores（状态管理：状态缓存、API 调用、数据转换）
    → Lib（基础设施：HTTP 客户端封装、Token 管理、路由守卫）
      → Core（纯工具函数：金额格式化、日期处理、分类图标映射等）
```

关键设计：
- 页面组件只负责 UI 渲染和事件分发，不直接调 API
- API 调用统一收拢在 Store 的 actions 中
- 金额在前端展示时除以 100 格式化为「元」，输入时乘以 100 转为「分」

### 6.3 前端框架选型

已确定使用 **Vue 3 + Vant 4**：

| 层 | 选型 | 说明 |
|----|------|------|
| 框架 | Vue 3.5+ | Composition API + `<script setup>` |
| 构建 | Vite 6+ | 快速 HMR |
| 语言 | TypeScript 5.x | 类型安全 |
| UI 组件 | Vant 4 | 轻量移动端组件库（~50KB gzipped），按需引入 |
| 路由 | Vue Router 4 | SPA 路由 |
| 状态管理 | Pinia | Vue 3 官方推荐 |
| HTTP | Axios | 拦截器处理 JWT |
| 图表 | ECharts 5 | 饼图、柱状图、折线图 |
| PWA | vite-plugin-pwa | manifest + Service Worker |

选择 Vant 4 的理由：轻量（~50KB gzipped）、移动端组件刚好够用（NavBar、Tabbar、Form、Field、Picker、DatetimePicker、Dialog 等）、按需引入 Tree-shaking、中文文档完善。

---

## 七、统计系统设计

### 7.1 统计维度

所有统计支持三个维度的筛选，可任意组合：

- **时间维度**：`start_time` ~ `end_time`（Unix 时间戳范围）
- **分类维度**：`category_ids`（逗号分隔的分类 ID 列表）
- **标签维度**：`tag_ids`（逗号分隔的标签 ID 列表）

统计计算时：
1. 先按筛选条件从 expense 表 + expense_tag_index 表联表查出符合条件的支出
2. 按目标维度（分类/标签/月份）做 GROUP BY 聚合
3. 计算每组的总额、占比、笔数

### 7.2 统计 SQL 示例

按分类统计（带时间范围和标签筛选）：

```sql
SELECT e.category_id,
       SUM(e.amount) AS total_amount,
       COUNT(*) AS transaction_count
FROM expense e
JOIN expense_tag_index eti ON e.id = eti.expense_id
WHERE e.uid = ? AND e.deleted = 0
  AND e.transaction_time BETWEEN ? AND ?
  AND eti.tag_id IN (?, ?)          -- 标签筛选（可选）
GROUP BY e.category_id
ORDER BY total_amount DESC;
```

### 7.3 图表类型

| 统计接口 | 图表类型 | 说明 |
|---------|---------|------|
| overview | 卡片数字 | 四个时间窗口的支出总额 |
| by_category | 饼图 | 各分类占比，可点击钻取详情 |
| by_tag | 饼图 | 各标签占比 |
| monthly | 柱状图 + 折线图 | 月度趋势，柱状图按分类堆叠 |

---

## 八、构建和部署

### 8.1 开发环境启动

```bash
# 后端（使用 Anaconda 管理 Python 环境）
conda create -n myaccounting python=3.12 -y
conda activate myaccounting
cd backend
pip install -r requirements.txt
uvicorn main:app --reload --host 0.0.0.0 --port 8080

# 前端
cd frontend
npm install
npm run dev
```

### 8.2 生产部署

**方式一：直接运行**

```bash
pip install -r requirements.txt
uvicorn main:app --host 0.0.0.0 --port 8080
```

**方式二：Docker Compose（推荐）**

```bash
# 修改 docker-compose.yml 中的 JWT 密钥后启动
docker compose up -d
```

**方式三：Docker 手动**

```bash
docker build -t ezexpense .
docker run -d \
  -p 8080:8080 \
  -v /path/to/data:/app/data \
  -e APP_SECURITY_JWT_SECRET=your-secret \
  ezexpense:latest
```

### 8.3 配置覆盖

所有 `config.yaml` 中的配置可通过环境变量覆盖：

```bash
APP_SERVER_PORT=9090
APP_DATABASE_PATH=/data/my_expense.db
APP_SECURITY_JWT_SECRET=your-secret-key
```

### 8.4 测试

**测试文件**：`backend/test_api.py`（26 个 API 集成测试）+ `backend/conftest.py`（pytest fixture）

**运行方式**：

```bash
# 1. 启动测试服务器（独立数据库，不影响开发数据）
cd backend
APP_DATABASE_PATH=./data/test.db uvicorn main:app --host 0.0.0.0 --port 8081 &

# 2. 运行测试
TEST_BASE=http://localhost:8081 python -m pytest test_api.py -v

# 3. 测试完成后清理
rm -f ./data/test.db
```

**测试覆盖（26 个用例）**：

| 模块 | 数量 | 覆盖 |
|------|------|------|
| 认证 | 6 | 注册(A/B/重复)、登录(成功/错误密码)、刷新令牌 |
| 分类 | 2 | 预设分类检查、创建自定义分类 |
| 标签 | 3 | 创建标签×2、同名标签被拒(400) |
| 支出 | 9 | 创建、列表、按标签筛选、按关键词筛选、更新、删除、恢复(标签恢复验证)、用户隔离、有支出分类不可删除 |
| 统计 | 4 | 概览、按分类、按标签、月度 |
| 用户 | 2 | 获取个人信息、修改昵称 |

**测试架构**：
- `conftest.py`：pytest 模块级 `_S` 共享状态类（token/category_id/tag_id/expense_id），`os.environ` 设置 test.db 隔离
- 测试通过 HTTP 直接调用 API，依赖**测试服务器已启动**
- 支持 `python test_api.py`（手动顺序执行）和 `pytest test_api.py -v`（推荐）

### 8.5 前端构建与生产模式

开发模式使用 Vite dev server（HMR），生产模式将前端构建产物部署到后端 static 目录，由 FastAPI 一体化服务：

```bash
# 构建前端（类型检查 + Vite 打包 → dist/）
cd frontend
npm install
npm run build

# 部署到后端 static 目录
rm -rf ../backend/static
cp -r dist ../backend/static

# 启动生产服务
cd ../backend
python -m uvicorn main:app --host 0.0.0.0 --port 8080
```

`main.py` 检测到 `static/` 目录存在时自动启用生产模式：
- `/assets/*` → 静态文件服务（Vite 构建的 JS/CSS）
- `/{path}` → SPA 回退（先尝试物理文件，不存在则返回 `index.html`）
- `/api/*` → API 路由（优先匹配，不受 SPA 回退影响）

开发模式下 `static/` 目录不存在，前端由 Vite dev server 独立服务，行为不变。

---

## 九、开发路线图

按 Phase 顺序推进，每个 Phase 结束都是可用的版本：

```
Phase 1 - 核心骨架（已完成 ✅）
  ✅ 项目目录结构搭建（backend/ + frontend/）
  ✅ 数据库模型定义（5 张表 + Alembic 迁移）
  ✅ 注册/登录/刷新令牌（JWT + bcrypt）
  ✅ 分类 CRUD
  ✅ 标签 CRUD
  ✅ 支出 CRUD（含标签关联维护）
  ✅ JWT 中间件 + 异常处理
  ✅ 预设分类数据（注册时自动创建）
  ✅ 26 个集成测试全部通过

Phase 2 - 前端 + 基本可用（已完成 ✅）
  ✅ Vue 3 + Vant 4 + Vite + TypeScript 项目搭建
  ✅ Pinia 状态管理 + Vue Router 路由 + 导航守卫
  ✅ Axios API 客户端 + JWT 拦截器 + 统一错误处理
  ✅ 登录页（含忘记密码：邮箱验证后自行设新密码）
  ✅ 注册页（用户名 + 邮箱 + 密码）
  ✅ 首页概览（今日/本周/本月/今年支出卡片 + 最近支出）
  ✅ 支出列表（游标分页、按分类/标签/时间筛选、关键词搜索、日期/金额排序）
  ✅ 记账表单（金额输入、分类选择、日期选择、标签多选含即时新增）
  ✅ 编辑支出（复用表单页）
  ✅ 分类管理（CRUD + emoji 图标 + 颜色选择）
  ✅ 标签管理（CRUD + 支出计数）
  ✅ 个人信息（改昵称、改密码、退出登录）
  ✅ 回收站（查看已删除支出、一键恢复）
  ✅ PWA manifest + Service Worker
  ✅ 通知系统（成功绿条/错误红条+弹窗/提示蓝条）
  ✅ 后端修复：排序改为按交易时间、智能搜索、分类渲染、邮箱字段、回收站 API

Phase 3 - 统计完善（已完成 ✅）
  ✅ 首页概览数据（今日/周/月/年）
  ✅ 按分类统计（饼图 + 下方列表可跳转支出列表）
  ✅ 按标签统计（饼图 + 下方列表可跳转支出列表）
  ✅ 月度趋势图（堆叠柱状图按分类着色 + 分类筛选）
  ✅ 标签月度趋势图（堆叠柱状图按标签着色）
  ✅ 饼图图例筛选联动下方列表（开关图例同步过滤排行）
  ✅ 时间范围选择（本月/近3月/近6月/今年/自定义支持单月）

容器化（已完成 ✅）
  ✅ Dockerfile 多阶段构建（Node 22 Alpine → Python 3.12 Slim）
  ✅ docker-compose.yml 一键部署
  ✅ FastAPI 生产模式自动服务静态文件 + SPA 回退
  ✅ .dockerignore 构建优化
  ✅ 数据卷持久化

Bug 修复轮次（已完成 ✅）
  ✅ API 层分类/标签返回改用 Pydantic schema（model_validate）
  ✅ CategoryPicker 编辑模式外部设值后显示文本同步
  ✅ ExpenseListPage watch route.query 响应式筛选
  ✅ statistics_service 零值时间戳 is not None 判断
  ✅ 饼图 click 取消直接跳转（与列表跳转冲突）
  ✅ 月度柱状图多行图例与 x轴动态间距
  ✅ 自定义时间支持单月分析（endDate 直接取月末日）
  ✅ 标签系统修复：删除支出/标签不物理删 TagIndex
  ✅ 创建/编辑支出时校验 tag 存在且未删除
  ✅ SQLite PRAGMA foreign_keys = ON（连接级事件监听）

代码优化轮次 1 — 消除冗余（已完成 ✅）
  ✅ N+1 查询修复：list_categories/tags 改 GROUP BY 批量查询
  ✅ 标签校验去重：提取 _validate_tags() 公共函数
  ✅ 配置默认值集中：config.py _DEFAULTS 字典替代散落默认值

代码优化轮次 2 — 分层清晰（已完成 ✅）
  ✅ API 响应校验统一：categories/tags 路由用 response_model 装饰器
  ✅ 前端类型派生：CategoryBrief/TagBrief 改用 Pick<>
  ✅ 查询参数工具：buildQueryParams() 集中处理
  ✅ 统计参数解析前置：API 层 _parse_ids()，Service 接收 list[int]
  ✅ 模型 Mixin：TimestampMixin + SoftDeleteMixin 消除重复列定义
  ✅ BaseSchema：基类统一 from_attributes，7 个 Response schema 继承
  ✅ statistics_service.monthly() 拆分为 5 个辅助函数
  ✅ StatisticsPage 590→377 行，提取 useECharts/usePeriodFilter composable
  ✅ Subquery 警告修复：.subquery() → .scalar_subquery()

代码优化轮次 3 — Bug 修复与测试（已完成 ✅）
  ✅ 26 集成测试（修复 24 + 新增 2）：email 字段同步 + pytest fixture + test.db 隔离 + 标签去重测试 + 恢复标签测试
  ✅ 支出列表无限滚动修复：van-list loading 守卫冲突
  ✅ 跨年数据显示：日期格式 YY/MM/DD，柱状图标签 YY/M
  ✅ 标签去重：create_tag 同名检查 + 数据库重复标签合并
  ✅ ExpenseListParams 去 |null 类型统一
  ✅ 区分初始加载量(15)与翻页量(10)，节约移动端资源

代码优化轮次 4 — 性能与质量打磨（已完成 ✅）
	后端：
	  ✅ N+1 排序修复：sort_categories/sort_tags 批量 IN 查询 + 内存映射（N 次→1 次往返）
	  ✅ Overview 查询合并：4 次 _sum_amount 顺序查询 → 1 次 CASE WHEN 条件聚合
	  ✅ 移除 create/update_expense 中冗余 db.refresh()（get_expense 立即重新查询）
	  ✅ _aggregate_tags 补充 deleted=0 过滤，避免已删除标签泄露到统计
	  ✅ tag_service 提取 _list_models 辅助函数，与 category_service 模式一致
	前端：
	  ✅ StatisticsPage resize 监听器加 onUnmounted 清理，防止多次进入页面累积
	  ✅ ExpenseFormPage dateDisplay ref→computed，消除手动同步
	  ✅ CategoryPicker selectedName ref+watch→computed
	  ✅ HomePage/TrashPage 复用 ExpenseCard 组件，消除 3 处内联重复布局
	  ✅ category/tag store CRUD 改为本地更新（push/findIndex/splice），不再全量重取
	  ✅ TrashPage handleRestore 直接调 API，跳过冗余的 store.restore 本地突变


Phase 4 - 体验打磨（按需）
  □ CSV 数据导出
  □ 支出搜索优化（备注全文搜索）
  □ 移动端交互动效
  □ 数据备份/恢复
  □ 预算提醒
```

---

## 十、设计原则总结

1. **金额整数存储**：全部以「分」为单位，int64，避免浮点精度问题
2. **时间统一 Unix 秒**：存储和计算用时间戳，展示时按用户时区格式化
3. **软删除统一模式**：核心表用 `deleted + deleted_at`，查询始终加 `deleted = 0`
4. **用户数据严格隔离**：所有表带 `uid`，所有查询带 `uid = ?`
5. **标签多对多**：通过关联表维护，修改支出时原子性更新
6. **事务保护**：涉及多表写入的操作（创建支出 + 标签关联）使用数据库事务
7. **分层清晰**：API → Service → Repository，职责不越界
8. **配置环境变量覆盖**：方便 Docker 部署和不同环境切换
